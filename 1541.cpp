#include <bits/stdc++.h>
 
using namespace std;
 
int main() {
    double a, b, c;
    while(cin >> a and a) {
        cin >> b >> c;
        cout << floor(sqrt(a * b / (c /100))) << endl;    
    }
    return 0;
}